@model IEnumerable<TOSS_UPGRADE.Models.Signatory_DepartmentTable>
@{
    DB_TOSSEntities SISDB = new DB_TOSSEntities();
}
@using TOSS_UPGRADE.Models;

<div class="col-md-12">
    <div class="table-responsive">
        <table class="table table-striped zero-configuration text-center dataTable scroll-horizontal table-smss-hover no-footer m-0 w-100">
            <thead class="text-center">
                <tr>
                    <th>Department</th>
                    <th>Department Code</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody class="text-center">
                @{
                    foreach (var Items in Model)
                    {
                        //var DeleteConfirmationMess = "Are you sure do you want to delete this Department?";
                        //var DeleteSuccesMess = "Department Deleted";

                        //var ViewEdit = "<button class=\"btn btn-icon btn-block black bg-addbutton border-0\" type=\"button\" onclick=\"AngularGlobalEdit('FMSignatoriesController','Get_UpdateDepartment','" + @Items.DepartmentID + "')\"><i class='ft-edit mr-1'></i><b>Edit</b></button>";
                        //var ViewDelete = "<button class=\"btn btn-icon white btn-block bg-headerbc  border-0\" type=\"button\" onclick=\"swalConfirmation('" + @DeleteConfirmationMess + "', '" + @Items.Department + "','" + @DeleteSuccesMess + "','AngularGlobalDelete(" + @Items.DepartmentID + ",&quot;Get_DeleteDepartment&quot;,&quot;FMSignatoriesController&quot;)')\"> <i class='ft-delete mr-1'></i><b>Delete<b></button>";
                        //var ActionContentProgram = ViewEdit + ViewDelete;
                        <tr>
                            <td>@Items.Department</td>
                            <td>@Items.DepartmentCode</td>

                            <td>
                                <button class="buttonEf iconhoverEdit"><i class="ft-edit"></i></button>
                                <button class="buttonEf iconhoverDelete"><i class="ft-delete"></i></button>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>


<script>

    $(function () {
        LoadDataTable();
        LoadPopOver();
    })
</script>

